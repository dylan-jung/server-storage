services:
  simple-file-storage:
    image: node:22
    volumes:
      - ./:/app
      - ./public:/app/public
    working_dir: /app
    command: npm run start
    restart: always
    
networks:
  default:
    external: true
    name: nginx-proxy-network
